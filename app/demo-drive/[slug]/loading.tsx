import { LoadingSection } from "@/components/shared/LoadingSection";

export default function DemoDriveLoading() {
  return (
    <LoadingSection
      title="Loading demo drive"
      description="Preparing the demo drive request form."
    />
  );
}
